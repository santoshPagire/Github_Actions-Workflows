name:  Concurrency-1
on: 
    workflow_dispatch:
        inputs:
            environment:
              description: "Environment"
              type: environment
              required: true
concurrency:
  group: Concurrency-1 
  cancel-in-progress: true

jobs:
  sleeps:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 30

            